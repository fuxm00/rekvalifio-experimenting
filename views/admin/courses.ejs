<%- include('./parts/_header') %>

<h2>Správa kurzů</h2>

<div>
    <a href="/admin/courses" class="<%= marked === "courses" ? "marked" : ""%>">Kurzy</a>
    <a href="/admin/courses/categories" class="<%= marked === "categories" ? "marked" : ""%>">Kategorie</a>
</div>

<%- include('./parts/_courseForm') %>

<div class="content-container">
    <h3>Seznam kurzů</h3>
    <% if ((courses.length >= 1)) { %>
        <%- include('./parts/_courseList') %>
    <% } else { %>
        <div class="content-container">
            <p>žádné</p>
        </div>
    <% } %>
</div>


<%- include('./parts/_footer') %>